# Shared secret — must match DEPLOY_SECRET in GitHub Actions
DEPLOY_SECRET=generate-a-strong-random-token
# TrueNAS API key — create at TrueNAS UI → Top-right user menu → API Keys
TRUENAS_API_KEY=your-truenas-api-key
# Comma-separated app names allowed to be updated
ALLOWED_APPS=pottery-log
